@model IEnumerable<Bug_Management_App.Models.Bugs>
@{
    ViewBag.Title = "BugsIndex";
}
<!----Filtros-->
<div>
    <ul class="nav filters-style nav-pills ">
        <li class="nav-item">
            <a class="nav-link">A</a>
        </li>
        <li class="nav-item">
            <a class="nav-link">E</a>
        </li>
        <li class="nav-item">
            <a class="nav-link">I</a>
        </li>
        <li class="nav-item">
            <a class="nav-link">O</a>
        </li>
        <li class="nav-item">
            <a class="nav-link">U</a>
        </li>
    </ul>
</div>

<div class="container-style container">
    <div class="scrollbar">
        <div class="row projects-dashboard">
            @foreach (var item in Model.Select((value, index) => new { value, index }))
            {
                <div class="bugs-column">
                    <a href="@Url.Action("ViewG", "Projects")" >
                        <div class="card">
                            <div class="row">
                                <h3></h3>
                            </div>
                        </div>
                    </a>
                </div>
            }
        </div>
    </div>
    <a class="float" href="@Url.Action("CreateBug", "Bugs", new {projectId = ViewBag.projectId })"><p class="plus">+</p></a>

</div>

